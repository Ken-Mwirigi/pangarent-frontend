services:
  frontend:
    build: .
    command: npm run dev -- --host 0.0.0.0
    ports:
      - "8080:8080"  
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true